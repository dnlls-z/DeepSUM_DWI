# DeepSUM_DWI
DeepSUM is a deep learning framework for MRI super-resolution that reconstructs high-resolution images from multiple low-resolution acquisitions. It combines image registration, patch-based training, and convolutional neural networks to enhance spatial resolution while preserving anatomical detail.
